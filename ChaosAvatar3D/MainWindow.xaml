<Window x:Class="ChaosAvatar3D.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChaosAvatar3D"
        xmlns:ht="http://helix-toolkit.org/wpf"
        mc:Ignorable="d"
        Title="ChaosAvatar 3D" Height="600" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Border Background="#AA000000" CornerRadius="15" BorderThickness="1">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#00d4ff" Offset="0"/>
                <GradientStop Color="#00ff88" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 3D Avatar and Chat -->
            <Grid Grid.Row="0">
                <ht:HelixViewport3D x:Name="helixViewport" MouseDown="Window_MouseDown">
                    <ht:DefaultLights/>
                    <ModelVisual3D Content="{Binding AvatarModel}"/>
                </ht:HelixViewport3D>

                <!-- Settings Button -->
                <Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Command="{Binding ToggleSettingsCommand}" Content="⚙️" FontSize="20" Foreground="White"/>

                <!-- Settings Panel -->
                <Border Background="#A0000000" CornerRadius="10" Padding="10" Margin="40"
                        Visibility="{Binding IsSettingsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <Button Content="Clear Chat" Command="{Binding ClearChatCommand}" Margin="0,5"/>
                        <Button Content="Close Application" Command="{Binding CloseWindowCommand}" Margin="0,5"/>
                    </StackPanel>
                </Border>

                <!-- Loading Indicator -->
                <Border Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" CornerRadius="10">
                    <TextBlock Text="Loading..." Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                </Border>

                <!-- Chat History -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10,0,10,10" MaxHeight="200" VerticalAlignment="Bottom">
                    <ItemsControl ItemsSource="{Binding ChatHistory}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="White" Margin="2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <!-- Input Area -->
            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,10,0"
                         Background="#33FFFFFF" Foreground="White" BorderThickness="0" Padding="5"/>
                <Button Grid.Column="1" Content="Send" Command="{Binding SendMessageCommand}" Padding="10,5"/>
            </Grid>
        </Grid>
    </Border>
</Window>